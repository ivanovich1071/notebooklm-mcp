# Git
.git
.gitignore

# Dependencies (will be installed in container)
node_modules

# Source files *are needed* for the build step. Do NOT exclude 'src'.
# The 'dist' folder will be created by the build process inside the container.
# src  <- УДАЛЕНО ЭТУ СТРОКУ

# Specific source file types *can* be excluded if they are not needed for the build,
# but excluding the entire 'src' directory prevents 'tsc' from finding input files.
*.ts
#!tsconfig*.json  # Это правило, вероятно, лишнее и путаное. tsconfig.json НУЖЕН.

# Tests (usually not needed in production container)
tests
*.test.ts
*.test.js
jest.config.*
coverage

# Development files
.env
.env.*
*.log
# Скорее всего, README.md не нужен в контейнере, можно исключить все .md
*.md
# !README.md  <- Можно оставить, если он нужен, но обычно не нужен. Закомментировано.

# IDE/Editor
.vscode
.idea
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Build artifacts not needed (the build process will create a fresh 'dist')
*.tsbuildinfo

# Documentation (optional in container)
docs
deployment

# Local data (should be mounted as volume, not copied)
library.json
*.enc.json

# Claude/development specific
.claude
scripts/*.ps1  # Скрипты PowerShell могут быть нужен, если они вызываются в процессе сборки.
*.output

# Husky hooks (not needed in container)
.husky

# Lock file should be included for reproducible builds
# package-lock.json <- УБЕДИТЕСЬ, ЧТО ЭТА СТРОКА НЕ ЗАКОММЕНТИРОВАНА
